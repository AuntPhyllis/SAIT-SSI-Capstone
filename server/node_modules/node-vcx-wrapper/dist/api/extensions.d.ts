/// <reference types="node" />
export interface IPackMessageData {
    data: Buffer;
    keys: string;
    sender: string;
}
export interface IUnpackMessageData {
    data: Buffer;
}
export declare class Extensions {
    /**
     * Pack message.
     *
     * Example:
     * ```
     * ```
     * @returns {Promise<string}
     */
    packMessage({ data, keys, sender }: IPackMessageData): Promise<Buffer>;
    /**
     * Unpack message.
     *
     * Example:
     * ```
     * ```
     * @returns {Promise<string}
     */
    unpackMessage(unpackData: IUnpackMessageData): Promise<Buffer>;
}
